# Python venv for the template generators (see README — build/make_*.py)
build/venv/
venv/
__pycache__/
*.py[cod]

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Node, if anything is ever added
node_modules/

# NOTE: templates/docx/*.docx and templates/xlsx/*.xlsx ARE committed on
# purpose. They are generated by build/make_*.py, but the site links to them
# directly as downloads, so they need to be present in a clone. Regenerate
# them rather than editing the binaries.
